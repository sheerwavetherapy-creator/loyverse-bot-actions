name: Resume Render Webhook Sender (LIVE)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Disabled after backlog storm. Type "I_UNDERSTAND_STORM_RISK" only after fixing the app backlog guard.'
        required: true
        default: 'NO'
      service_id:
        description: 'Optional: Render Service ID to resume (if blank uses repo secret SERVICE_ID)'
        required: false
        default: ''

jobs:
  resume-live-webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Make script executable
        run: chmod +x scripts/resume-render-webhook-live.sh

      - name: Require explicit live confirmation
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "I_UNDERSTAND_STORM_RISK" ]; then
            echo "Live webhook resume is disabled after the 2026-09-09 historical backlog storm."
            echo "Do not re-enable Telegram sends until the Render app ignores historical Loyverse backlog events on startup."
            exit 1
          fi

      - name: Resume Render with webhook sends enabled
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          LOYVERSE_WEBHOOK_SECRET: ${{ secrets.LOYVERSE_WEBHOOK_SECRET }}
          TRIGGER_DEPLOY: 'true'
        run: |
          set -euo pipefail
          if [ "${{ github.event.inputs.service_id }}" != "" ]; then
            SERVICE_ID="${{ github.event.inputs.service_id }}"
          else
            SERVICE_ID="${{ secrets.SERVICE_ID }}"
          fi
          export SERVICE_ID
          ./scripts/resume-render-webhook-live.sh
